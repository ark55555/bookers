<h1>Editing Book</h1>
<% if @book.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= @book.errors.count %>errorprohibited this book from being saved:
    </h2>
    <ul>
      <li><% @book.errors.full_messages.each do |message| %>
          <%= message %>
      </li>
    </ul>
  </diV>
     <% end %>
<% end %>
<%= form_with model:@book, local: true do |f| %>
  <div class="field">
    <label for="book_title">Title</label>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <label for="book_body">Body</label>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Update Book' %>
  </div>
  
<% end %>
<a><%= link_to "Show", book_path(@book.id) %></a>
<text>|</text>
<a><%= link_to "Back", books_path(@book.id) %></a>